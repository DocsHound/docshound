mutation updateGlobalSharedUserCredential(
  $provider: Provider!
  $credentialsJSON: JSONObject!
) {
  updateGlobalSharedUserCredential(
    provider: $provider
    credentialsJSON: $credentialsJSON
  ) {
    id
  }
}
